[project]
name = "scvminer"
version = "0.1.0"
description = "A Python project managed with uv."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "gensim>=4.3.3",
    "numpy>=1.26.0",
    "slither-analyzer>=0.11.5",
]
